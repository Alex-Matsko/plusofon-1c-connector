version: '3.9'

services:
  connector:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - connector_data:/data
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  connector_data:
